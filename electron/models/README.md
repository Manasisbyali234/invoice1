# YOLO Custom Model (Optional)

Place your trained YOLOv8 model here as `copy_type.pt`.

## Model Classes
- 0 = original
- 1 = duplicate
- 2 = triplicate

## Without custom model
The system automatically falls back to EasyOCR visual detection,
which handles stamps, watermarks, and non-selectable text in PDFs.
